@page "/DynamicFormApprovedMainList/dynamicFormsApproved/{SessionId:guid}"
@page "/DynamicFormApprovedMainList/dynamicFormsApproved/{SessionId:guid}/{SessionIds:guid}"
<DynamicForms SessionId="@SessionId" SessionIds="@SessionIds"></DynamicForms>
@code {
    [Parameter]
    public Guid? SessionIds { get; set; }
    [Parameter]
    public Guid? SessionId { get; set; }
}