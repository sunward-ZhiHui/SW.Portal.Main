@page "/DynamicMasterSetupList/dynamicFormList/{SessionId:guid}"
@page "/DynamicMasterSetupList/dynamicFormList/{SessionId:guid}/{FormSessionId:guid}"
<DynamicFormList SessionId="@SessionId" FormSessionId="@FormSessionId"></DynamicFormList>
@code {
    [Parameter]
    public Guid? SessionId { get; set; }
    [Parameter]
    public Guid? FormSessionId { get; set; }
}
