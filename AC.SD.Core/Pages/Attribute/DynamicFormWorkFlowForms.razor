@page "/DynamicFormWorkFlowList/dynamicForms/{SessionId:guid}"
@page "/DynamicFormWorkFlowList/dynamicForms/{SessionId:guid}/{SessionIds:guid}"
<DynamicForms SessionId="@SessionId" SessionIds="@SessionIds"></DynamicForms>
@code {
    [Parameter]
    public Guid? SessionIds { get; set; }
    [Parameter]
    public Guid? SessionId { get; set; }
}