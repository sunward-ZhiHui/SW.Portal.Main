@using Application.Commands;
@using Application.Queries;
@using Application.Response;
@using MediatR
@using System.Dynamic
@using global::Core.Entities;
@using global::Core.Entities.Views;
@using global::Core.EntityModels;
@inject IMediator Mediator
@using DevExpress.Export
@using global::Core.Repositories.Query;
@inject IJSRuntime jsRuntime;
@inject NavigationManager NavigationManager
@inject AC.SD.Core.Services.ToastService toastService
@inject ILocalStorageService<ApplicationUser> _localStorageService
@* @inject AC.SD.Core.Helpers.TreeGenerateRecursive _treeGenerateRecursive *@
@implements IAsyncDisposable

<div class="card w-100" style="margin-bottom:10px;">
    <div class="card-body p-0">
        <DxMenu CollapseItemsToHamburgerMenu="true"
                CollapseItemToIconMode="MenuCollapseItemToIconMode.Sequentially"
                DisplayMode="MenuDisplayMode.Desktop" style="color:chocolate;">
            <Items>
                <DxMenuItem Text="Add" IconCssClass="fa fa-add" Click="@addNew" Enabled="@AddEnabled" />
                <DxMenuItem Text="Edit" IconCssClass="fa fa-pencil" Click="@addEdit" Enabled="@EditEnabled" />
                <DxMenuItem Text="Delete" IconCssClass="fa fa-remove" Click="@addDelete" Enabled="@DeleteEnabled" />
                <DxMenuItem Text="Attachment" IconCssClass="fa fa-paperclip" Click="@addAttachment" Enabled="@AttachmentEnabled" />
                <DxMenuItem Text="Email" IconCssClass="fa fa-envelope" Click="@addDetailEmailCreate" Enabled="@EditEnabled" />
                <DxMenuItem Text="View Email" IconCssClass="fa fa-eye" Click="@addDetailEmailView" Enabled="@EditEnabled" />
            </Items>
        </DxMenu>
    </div>
</div>


<DxPopup @ref="popup" HeaderText="CRT" ShowCloseButton="true" @bind-Visible="@PopupVisible">
    <div class="text-center">
        <p>@errorMessage</p>
        <DxButton Text="OK" Click="@(() => ClosePopup())" />
    </div>
</DxPopup>
<DxPopup @ref="popup" HeaderText="CRT" ShowCloseButton="true" @bind-Visible="@PopupVisible">
    <div class="text-center">
        <p>@DeleteerrorMessage</p>
        <DxButton Text="OK" Click="@(() => ClosePopup())" />
    </div>
</DxPopup>

<DxPopup @bind-Visible="@MessageVisible" ShowCloseButton="true" HeaderText="@MessageHeader">
    <BodyTemplate Context="PopupContext">
        <div class="@((MessageHeader == "Error" ? "TxtRed" : "TxtGreen"))" style="text-align:center; padding: 10px; font-size: 15px;">
            <span> @MessageBody</span>
        </div>
    </BodyTemplate>
</DxPopup>

<DxLoadingPanel @bind-Visible="PanelVisible"
                IsContentBlocked="true"
                ApplyBackgroundShading="true"
                IndicatorAreaVisible="false"
                Text="Fetching Data...">

    <DxGrid @ref="SMGrid" Data="@_dynamicFormSectionData" EditModelSaving="@OnEditModelSaving" DataItemDeleting="@OnDeleteDataItem" EditMode="GridEditMode.EditForm" ShowGroupPanel="true" ShowGroupedColumns="true" ShowSearchBox="true"
            FilterMenuButtonDisplayMode="GridFilterMenuButtonDisplayMode.Always"
            ColumnResizeMode="GridColumnResizeMode.NextColumn"
            PagerNavigationMode="PagerNavigationMode.InputBox"
            KeyFieldName="RegistrationRequestComittmentLetterId"
            PageSizeSelectorVisible="true"
            PageSizeSelectorAllRowsItemVisible="true"
            PageSizeSelectorItems="@(new int[] { 5,10,20 })"
            PageSize="20"
            CssClass="ch-360"
            FocusedRowEnabled="true"
            RowClick="OnRowClick"
            AllowSelectRowByClick="true"
            CustomizeElement="Grid_CustomizeElement"
            CustomizeEditModel="Grid_CustomizeEditModel"
            AutoExpandAllGroupRows="true"
            SelectionMode="GridSelectionMode.Single"
            SelectAllCheckboxMode="GridSelectAllCheckboxMode.AllPages"
            RowDoubleClick="OnRowDoubleClick" SelectedDataItemsChanged="@SelectedDataItemsChanged">


        <Columns>
            <DxGridDataColumn FieldName="RegistrationRequestComittmentLetterId" Visible="false" />
            <DxGridDataColumn FieldName="IsEmailCreate" Caption="Is Email Create" MinWidth="40" />
            <DxGridDataColumn FieldName="CommittmentDate" Caption="Committment Date" MinWidth="40">
                <CellDisplayTemplate>
                    @{
                        var item = (RegistrationRequestComittmentLetter)context.DataItem;
                        var startDate = string.Empty;
                        if (item.CommittmentDate != DateTime.MinValue)
                        {
                            if (item.CommittmentDate != null)
                            {
                                startDate = item.CommittmentDate.Value.ToString("dd-MMM-yyyy");
                            }
                        }
                    }
                    @startDate
                </CellDisplayTemplate>
            </DxGridDataColumn>
            <DxGridDataColumn FieldName="DepartmentName" Caption="Department" MinWidth="40" />
            <DxGridDataColumn FieldName="CommitmentInformation" Caption="Commitment Information" MinWidth="40" />
            <DxGridDataColumn FieldName="CommitmentTime" Caption="Commitment Time" MinWidth="40">
                <CellDisplayTemplate>
                    @{
                        var item = (RegistrationRequestComittmentLetter)context.DataItem;
                        var startDate = string.Empty;
                        if (item.CommitmentTime != DateTime.MinValue)
                        {
                            if (item.CommitmentTime != null)
                            {
                                startDate = item.CommitmentTime.Value.ToString("dd-MMM-yyyy");
                            }
                        }
                    }
                    @startDate
                </CellDisplayTemplate>
            </DxGridDataColumn>
            <DxGridDataColumn FieldName="StatusCode" Caption="Status" MinWidth="40" />
            @* <DxGridDataColumn FieldName="ModifiedBy" Caption="Modified By" MinWidth="50" />
            <DxGridDataColumn FieldName="ModifiedDate" Caption="Modified Date" Width="180" /> *@
            <DxGridCommandColumn Width="160px" Visible="false" />
        </Columns>
        <EditFormTemplate Context="EditFormContext">
            @{
                var types = (RegistrationRequestComittmentLetter)EditFormContext.EditModel;
            }
            <DxFormLayout CssClass="w-100">
                <DxFormLayoutItem Caption="Is CommitmentDate" ColSpanMd="6">
                    <DxCheckBox @bind-Checked="@types.IsCommitmentDate"></DxCheckBox>
                </DxFormLayoutItem>
                <DxFormLayoutItem Caption="Date" ColSpanMd="6" Visible="@types.IsCommitmentDate">
                    <DxDateEdit NullText="Date" @bind-Date="@types.CommittmentDate" />
                </DxFormLayoutItem>
                <DxFormLayoutItem Caption="Department" ColSpanMd="6">
                    <DxComboBox Data="@_departmentItems"
                                NullText="Select Department..."
                                SearchMode="ListSearchMode.AutoSearch"
                                SearchFilterCondition="ListSearchFilterCondition.Contains"
                                TextFieldName="DepartmentName"
                                ValueFieldName="DepartmentId"
                                @bind-Value="@types.ActionByDeptId">
                        <Columns>
                            <DxListEditorColumn FieldName="@nameof(ViewDepartment.DepartmentName)"
                                                Caption="Department Name"
                                                Width="100px" />
                            <DxListEditorColumn FieldName="@nameof(ViewDepartment.DepartmentCode)"
                                                Caption="Code"
                                                Width="100px" />
                            <DxListEditorColumn FieldName="@nameof(ViewDepartment.Description)"
                                                Caption="Description"
                                                Width="100px" />
                            <DxListEditorColumn FieldName="@nameof(ViewDepartment.PlantCode)"
                                                Caption="Company" Width="100px" />
                            <DxListEditorColumn FieldName="@nameof(ViewDepartment.Division)"
                                                Caption="Division" Width="100px" />

                        </Columns>
                    </DxComboBox>
                    <ValidationMessage For="@(() => types.ActionByDeptId)" />
                </DxFormLayoutItem>

                <DxFormLayoutItem Caption="Commitment Information" ColSpanMd="12">
                    <DxMemo @bind-Text="@types.CommitmentInformation" Rows="5" />
                </DxFormLayoutItem>
                <DxFormLayoutItem Caption="Commitment Date" ColSpanMd="6">
                    <DxDateEdit NullText="Commitment Date" @bind-Date="@types.CommitmentTime" />
                </DxFormLayoutItem>
                <DxFormLayoutItem Caption="Status" ColSpanMd="6">
                    <DxComboBox Data="@_stausItems"
                                NullText="Select Status..."
                                SearchMode="ListSearchMode.AutoSearch"
                                SearchFilterCondition="ListSearchFilterCondition.Contains"
                                TextFieldName="CodeValue"
                                ValueFieldName="CodeId"
                                @bind-Value="@types.StatusCodeId"
                                ShowValidationIcon="true">
                    </DxComboBox>
                    <ValidationMessage For="@(() => types.StatusCodeId)" />
                </DxFormLayoutItem>
            </DxFormLayout>
        </EditFormTemplate>

        <TotalSummary>
            <DxGridSummaryItem SummaryType="GridSummaryItemType.Count" FieldName="DueDate" />
        </TotalSummary>
    </DxGrid>
</DxLoadingPanel>

<DxPopup @bind-Visible="@Blukdeletepopup"
         ShowFooter="true"
         HeaderText="Delete"
         Closed="EulaPopupClosed"
         ShowCloseButton="true"
         Width="300px">
    <BodyContentTemplate>
        <p>
            Do you want to delete?
        </p>
    </BodyContentTemplate>
    <FooterContentTemplate Context="Context">
        <DxButton style="margin-right: 10px;" RenderStyle="ButtonRenderStyle.Primary" Text="NO" Click="Context.CloseCallback" />
        <DxButton RenderStyle="ButtonRenderStyle.Secondary" Text="Yes" Click="bulkDelete" />
    </FooterContentTemplate>
</DxPopup>
<DxPopup @bind-Visible="@AttachmentPopUp"
         ShowFooter="false"
         HeaderText="Attachment"
         CloseOnOutsideClick="false"
         ShowCloseButton="true"
         MinWidth="300" MinHeight="200" MaxWidth="1000px" MaxHeight="800">
    <BodyContentTemplate>
        <AC.SD.Core.Pages.UploadDocuments.UploadDocuments SessionId="@_selectedItems.SessionId" RevokeBack="CloseAttachmentPopup"></AC.SD.Core.Pages.UploadDocuments.UploadDocuments>
    </BodyContentTemplate>
    <FooterContentTemplate Context="Context">
        <DxButton RenderStyle="ButtonRenderStyle.Secondary" Text="Close" Click="@(() => CloseAttachmentPopup())" />
    </FooterContentTemplate>
</DxPopup>
<DxPopup @bind-Visible="@EmailAddpopupForm"
         ShowFooter="false"
         ShowCloseButton="true"
         HeaderText="Create Email"
         CloseOnOutsideClick="false"
         CloseOnEscape="false"
         Width="1000px">
    <BodyContentTemplate>
        <RegistrationRequestCreateEmailForm Type="ComittmentLetter" applicationUser="@applicationUser" DepartmentUniqueSessionId="@_selectedItems?.SessionId" _registrationRequestData="@_registrationRequestDatas" _RegistrationRequestDepartmentData="@_dynamicFormSectionDatas" RevokeBack="AddpopupFormClose"></RegistrationRequestCreateEmailForm>
    </BodyContentTemplate>
</DxPopup>
@code {
    bool EmailAddpopupForm { get; set; } = false;
    private List<CodeMaster> _stausItems;
    private List<ViewDepartment> _departmentItems;
    [Parameter]
    public RegistrationRequestProgressByRegistrationDepartment _registrationRequestData { get; set; }
    bool AttachmentEnabled { get; set; } = false; bool AttachmentPopUp { get; set; } = false;
    bool PanelVisible { get; set; }
    IReadOnlyList<object> SelectedDataItems { get; set; }
    private string gridHeight = "calc(100vh - 230px - 143px)";
    private List<RegistrationRequestComittmentLetter>? _dynamicFormSectionData;
    private List<RegistrationRequestAssignmentOfJob>? _dynamicFormSectionDatas;
    IGrid? SMGrid { get; set; }
    private RegistrationRequestComittmentLetter _selectedItems;
    bool DeleteEnabled { get; set; } = false;
    bool ExportSelectedRowsOnly { get; set; }
    DxPopup? popup;
    bool PopupVisible { get; set; } = false;
    string? errorMessage;
    string? DeleteerrorMessage;
    bool MessageVisible { get; set; }
    string MessageHeader = "Error";
    string MessageBody = "";
    private bool loading;
    bool Blukdeletepopup { get; set; } = false;
    bool EditEnabled { get; set; } = false;
    bool AddEnabled { get; set; } = false;
    private RegistrationRequestComittmentLetter _selectlst;
    [Parameter]
    public ApplicationUser applicationUser { get; set; }
    [Parameter]
    public RegistrationRequest _registrationRequestDatas { get; set; }
    protected override async Task OnInitializedAsync()
    {
        _departmentItems = await Mediator.Send(new GetAllDepartmentQuery());
        _stausItems = await Mediator.Send(new GetAllCodeMasterQuery("ComittmentLetterStatus"));
        await UpdateDataAsync();
    }
    public async Task loadChildData(RegistrationRequestProgressByRegistrationDepartment dynamicFormIds)
    {
        _registrationRequestData = dynamicFormIds;
        await UpdateDataAsync();
    }
    async Task Grid_CustomizeEditModel(GridCustomizeEditModelEventArgs e)
    {
        var navitems = (RegistrationRequestComittmentLetter)e.EditModel;
        if (e.IsNew)
        {
        }
        else
        {
        }
    }
    void CloseAttachmentPopup()
    {
        InvokeAsync(UpdateDataAsync);
        AttachmentPopUp = false;
    }
    void addAttachment()
    {
        AttachmentPopUp = true;
    }
    async Task UpdateDataAsync()
    {
        try
        {
            PanelVisible = true; AddEnabled = false;
            EditEnabled = false; DeleteEnabled = false; AttachmentEnabled = false;
            if (_registrationRequestData != null && _registrationRequestData.RegistrationRequestProgressByRegistrationDepartmentId > 0)
            {
                AddEnabled = true;
                _dynamicFormSectionData = await Mediator.Send(new GetRegistrationRequestComittmentLetter(_registrationRequestData.RegistrationRequestProgressByRegistrationDepartmentId));
                if (_dynamicFormSectionData.Count > 0)
                {
                    _selectedItems = _dynamicFormSectionData.FirstOrDefault();
                    EditEnabled = true; AttachmentEnabled = true;
                    DeleteEnabled = true;
                }
            }
            else
            {
                _dynamicFormSectionData = new List<RegistrationRequestComittmentLetter>();
            }
            PanelVisible = false;
            StateHasChanged();
        }
        catch (Exception ex)
        {

        }
    }
    void OnCustomizeCell(GridExportCustomizeCellEventArgs args)
    {
        if (args.ColumnFieldName == "ContactName" && args.AreaType == SheetAreaType.DataArea)
            args.Formatting.Font = new XlCellFont() { Italic = true };
        args.Handled = true;
    }

    async Task OnEditModelSaving(GridEditModelSavingEventArgs e)
    {

        var editModel = (RegistrationRequestComittmentLetter)e.EditModel;

        if (e.IsNew)
        {
            var createReq = new InsertorUpdateRegistrationRequestComittmentLetter
                {
                    ActionByDeptId = editModel.ActionByDeptId,
                    CommitmentInformation = editModel.CommitmentInformation,
                    CommitmentTime = editModel.CommitmentTime,
                    CommittmentDate = editModel.IsCommitmentDate == true ? editModel.CommittmentDate : null,
                    RegistrationRequestProgressByRegistrationDepartmentId = _registrationRequestData.RegistrationRequestProgressByRegistrationDepartmentId,
                    SessionId = Guid.NewGuid(),
                    AddedByUserId = applicationUser.UserID,
                    ModifiedUserId = applicationUser.UserID,
                    ModifiedDate = DateTime.Now,
                    StatusCodeId = editModel.StatusCodeId,
                    AddedDate = DateTime.Now,
                    IsCommitmentDate = editModel.IsCommitmentDate,
                };
            try
            {
                var result = await Mediator.Send(createReq);
                toastService.ShowToast("Record save Successfully", AC.SD.Core.Services.ToastLevel.Success);
            }
            catch (Exception eq)
            {

                errorMessage = eq.Message;
                PopupVisible = true;

            }
            await UpdateDataAsync();


        }
        else
        {
            var UpdateReq = new InsertorUpdateRegistrationRequestComittmentLetter
                {
                    ActionByDeptId = editModel.ActionByDeptId,
                    CommitmentInformation = editModel.CommitmentInformation,
                    CommitmentTime = editModel.CommitmentTime,
                    CommittmentDate = editModel.IsCommitmentDate == true ? editModel.CommittmentDate : null,
                    RegistrationRequestProgressByRegistrationDepartmentId = _registrationRequestData.RegistrationRequestProgressByRegistrationDepartmentId,
                    RegistrationRequestComittmentLetterId = editModel.RegistrationRequestComittmentLetterId,
                    SessionId = Guid.NewGuid(),
                    AddedByUserId = editModel.AddedByUserId,
                    ModifiedUserId = applicationUser.UserID,
                    ModifiedDate = DateTime.Now,
                    StatusCodeId = editModel.StatusCodeId,
                    AddedDate = editModel.AddedDate,
                    IsCommitmentDate = editModel.IsCommitmentDate,
                };
            var result = await Mediator.Send(UpdateReq);
            toastService.ShowToast("Record Updated Successfully", AC.SD.Core.Services.ToastLevel.Success);
            await UpdateDataAsync();

        }

    }
    private async void onShowPopupMessage(string headerText, string bodyText)
    {

        MessageVisible = true;
        MessageBody = bodyText;
        MessageHeader = headerText;
        loading = false;
        StateHasChanged();
    }
    async void ClosePopup()
    {
        SelectedDataItems = null;
        Blukdeletepopup = false;
        PopupVisible = false;
    }
    void EulaPopupClosed(PopupClosedEventArgs args)
    {
        SelectedDataItems = null;
        Blukdeletepopup = false;
        PopupVisible = false;
    }
    async Task OnDeleteDataItem(GridDataItemDeletingEventArgs e)
    {
        try
        {
            var editModel = (RegistrationRequestComittmentLetter)e.DataItem;
            var request = new DeleteRegistrationRequestComittmentLetter(editModel);
            await Mediator.Send(request);
            await UpdateDataAsync();
            SMGrid.SetFocusedRowIndex(0);
        }
        catch (Exception ex)
        {

        }
    }
    void Grid_CustomizeElement(GridCustomizeElementEventArgs e)
    {
        if (e.ElementType == GridElementType.DataRow && e.VisibleIndex % 2 == 1)
        {
            e.CssClass = "alt-item";
        }

    }
    void Onclick(DataGridSelection<RegistrationRequestComittmentLetter> e)
    {
    }

    void OnRowClick(GridRowClickEventArgs e)
    {
        var UniqueId = e.Grid.GetRowValue(e.VisibleIndex, "RegistrationRequestComittmentLetterId");
        _selectedItems = _dynamicFormSectionData.FirstOrDefault(f => f.RegistrationRequestComittmentLetterId == (long?)UniqueId);
        DeleteEnabled = true; AttachmentEnabled = true;
        StateHasChanged();

    }
    void OnRowDoubleClick(GridRowClickEventArgs e)
    {
        OnRowClick(e);
        addEdit();
    }
    void addNew()
    {
        SMGrid.StartEditNewRowAsync();

    }
    void addEdit()
    {
        if (SelectedDataItems != null)
        {

            if (SelectedDataItems.Count == 1)
            {

                foreach (object item in SelectedDataItems)
                {
                    if (item is RegistrationRequestComittmentLetter dataselectItems)
                    {
                        _selectlst = _dynamicFormSectionData.FirstOrDefault(f => f.RegistrationRequestComittmentLetterId == dataselectItems.RegistrationRequestComittmentLetterId);
                    }
                }
                SMGrid.StartEditDataItemAsync(_selectlst);
            }
            else
            {
                SMGrid.StartEditDataItemAsync(_selectedItems);
            }
        }

    }
    void addDelete()
    {
        Blukdeletepopup = true;
    }
    async Task bulkDelete()
    {
        try
        {
            if (SelectedDataItems != null)
            {
                foreach (object item in SelectedDataItems)
                {
                    if (item is RegistrationRequestComittmentLetter selectData)
                    {

                        var request = new DeleteRegistrationRequestComittmentLetter(selectData);
                        await Mediator.Send(request);
                        Blukdeletepopup = false;
                        toastService.ShowToast("Record deleted Successfully", AC.SD.Core.Services.ToastLevel.Success);
                        SMGrid.SetFocusedRowIndex(0);
                    }

                }
            }
        }
        catch (Exception eq)
        {
            Blukdeletepopup = false;
            toastService.ShowToast("The Selected Record Do not deleted", AC.SD.Core.Services.ToastLevel.Error);
        }

        await UpdateDataAsync();


    }
    void SelectedDataItemsChanged(IReadOnlyList<object> SelectedToItems)
    {
        SelectedDataItems = SelectedToItems;
        if (SelectedDataItems.Count > 1)
        {
            EditEnabled = false;
        }
        else
        {
            EditEnabled = true;
        }

    }
    void AddpopupFormClose()
    {
        InvokeAsync(UpdateDataAsync);
        EmailAddpopupForm = false;
        StateHasChanged();
    }
    async Task addDetailEmailView()
    {
        if (_selectedItems != null)
        {
            var _selectedItemsDAta = await Mediator.Send(new GetActivityEmailTopicsExits(_registrationRequestData.SessionId));
            if (_selectedItemsDAta != null)
            {
                if (_selectedItemsDAta.IsDraft == true)
                {
                    var url = NavigationManager.BaseUri + "EmailDrafts";
                    await jsRuntime.InvokeVoidAsync("window.open", url, "_blank");
                }
                if (_selectedItemsDAta.IsDraft == false)
                {
                    var url = NavigationManager.BaseUri + "ViewEmail/" + _selectedItemsDAta.EmailTopicSessionId;
                    await jsRuntime.InvokeVoidAsync("window.open", url, "_blank");
                }
                if (_selectedItemsDAta.IsDraft == null)
                {
                    await addDetailEmailCreate();
                }
            }
            else
            {
                await addDetailEmailCreate();
            }
        }
    }
    async Task addDetailEmailCreate()
    {
        if (_selectedItems != null)
        {
            if (_selectedItems.IsEmailCreate == false)
            {
                _dynamicFormSectionDatas = new List<RegistrationRequestAssignmentOfJob>();
                RegistrationRequestAssignmentOfJob registrationRequestAssignmentOfJob = new RegistrationRequestAssignmentOfJob();
                registrationRequestAssignmentOfJob.RegistrationRequestAssignmentOfJobId = _selectedItems.RegistrationRequestComittmentLetterId;
                registrationRequestAssignmentOfJob.CommittmentDate = _selectedItems.CommittmentDate;
                registrationRequestAssignmentOfJob.CommitmentInformation = _selectedItems.CommitmentInformation;
                registrationRequestAssignmentOfJob.IsEmailCreateDone = true;
                registrationRequestAssignmentOfJob.CommitmentTime = _selectedItems.CommitmentTime;
                registrationRequestAssignmentOfJob.CommitmentStatus = _selectedItems.StatusCode;
                registrationRequestAssignmentOfJob.Type = "ComittmentLetter";
                registrationRequestAssignmentOfJob.DepartmentId = _selectedItems.ActionByDeptId;
                registrationRequestAssignmentOfJob.DepartmentName = _selectedItems.DepartmentName;
                registrationRequestAssignmentOfJob.DepartmentUniqueSessionId = _selectedItems.SessionId;
                _dynamicFormSectionDatas.Add(registrationRequestAssignmentOfJob);
                EmailAddpopupForm = true;
            }
        }
    }
    public async ValueTask DisposeAsync()
    {

    }
}
