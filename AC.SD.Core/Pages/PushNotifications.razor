@page "/push-notifications"

<button @onclick="InitializeFirebaseMessaging">Initialize Firebase Messaging</button>

@code {
    private async Task InitializeFirebaseMessaging()
    {
        //var messaging = firebase.messaging();

        //try
        //{
        //    await messaging.requestPermission();
        //    console.log('Notification permission granted.');
        //    const token = await messaging.getToken();
        //    console.log('Token:', token);
        //    // Send this token to your server to associate it with the user.
        //}
        //catch (error)
        //{
        //    console.error('Error obtaining token:', error);
        //}
    }
}
