
@page "/consumptionsLines"
@using Application.Queries;
@using MediatR;
@using global::Core.Entities.Views;
@inject IMediator Mediator
<style>
    .dxbl-loading-panel
    {
        display: initial !important;
    }
</style>

<DxLoadingPanel @bind-Visible="PanelVisible"
                IsContentBlocked="true"
                ApplyBackgroundShading="true"
                IndicatorAreaVisible="false"
                Text="Fetching Data..."
               >
    <DxGrid Data="@consumptionLines" FilterMenuButtonDisplayMode="GridFilterMenuButtonDisplayMode.Always" ShowGroupPanel="true" ShowGroupedColumns="true" ShowSearchBox="true"
        PageSize="25"
        PagerNavigationMode="PagerNavigationMode.InputBox" PageSizeSelectorVisible="true"
        PageSizeSelectorAllRowsItemVisible="true"
        PageSizeSelectorItems="@(new int[] { 5,10,20 })"
        CssClass="w-100 conswidth" style="height: calc(100vh - 438px);"
        SelectionMode="GridSelectionMode.Multiple"
        SelectAllCheckboxMode="GridSelectAllCheckboxMode.AllPages"
        CustomizeElement="Grid_CustomizeElement"
        AutoExpandAllGroupRows="true" >
    <Columns>
            <DxGridDataColumn FieldName="ItemNo" MinWidth="200" Caption="MaterialNo" FixedPosition="GridColumnFixedPosition.Left"/>
            <DxGridDataColumn FieldName="Description" MinWidth="180" FixedPosition="GridColumnFixedPosition.Left" />
            <DxGridDataColumn FieldName="LotNo" MinWidth="180" />
            <DxGridDataColumn FieldName="SubLotNo" MinWidth="180" />
            <DxGridDataColumn FieldName="DrumNo" MinWidth="180" />
            <DxGridDataColumn FieldName="QCRefNo" MinWidth="180" />
            <DxGridDataColumn FieldName="BatchNo" MinWidth="180" />
            <DxGridDataColumn FieldName="Quantity" MinWidth="180" />
            <DxGridDataColumn FieldName="UOM" MinWidth="150" />
            <DxGridDataColumn FieldName="PostedtoNav" MinWidth="180" />
    </Columns>

</DxGrid>
</DxLoadingPanel>
@code {
    bool PanelVisible { get; set; }
    [Parameter]
    public ViewConsumptionEntry consumptionEntry { get; set; }
    private List<ViewConsumptionLines> consumptionLines;
    IGrid Grid { get; set; }
    protected override async Task OnInitializedAsync()
    {
        PanelVisible = true;
        var model = await Mediator.Send(new GetAllConsumptionLinesQuery());
        consumptionLines = model
            .Where(i => i.ConsumptionEntryID == consumptionEntry.ConsumptionEntryID)
            .ToList();
        PanelVisible = false;
    }
    void Grid_CustomizeElement(GridCustomizeElementEventArgs e)
    {
        if (e.ElementType == GridElementType.DataRow && e.VisibleIndex % 2 == 1)
        {
            e.CssClass = "alt-item";
        }
        //if (e.ElementType == GridElementType.HeaderCell)
        //{
        //    e.Style = "background-color: rgba(0, 0, 0, 0.08)";
        //    e.CssClass = "header-bold";
        //}
    }
}
